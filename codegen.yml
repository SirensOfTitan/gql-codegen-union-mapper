overwrite: true
schema: "schema.graphql"
generates:
  resolver-types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      mappers:
        Movie: ./types#MovieEntity
